#pragma once

// C++ Includes
#include <array>

// SDL Includes
#include <SDL.h>

namespace BakedEngine
{

extern bool init(uint32_t subSystemFlags = SDL_INIT_EVERYTHING);

} // End of namespace BakedEngine
